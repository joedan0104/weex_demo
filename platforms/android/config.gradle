ext {

    android = [
            compileSdkVersion: 27,
            buildToolsVersion: "27.0.3",
            supportSdkVersion: "25.0.1",
            minSdkVersion    : 15,
            targetSdkVersion : 26,
            versionCode      : 1,
            versionName      : "1.0"
    ]

    /**
     * maven库公共资源
     */
    maven_public = [
            "releaseUrl" : 'http://192.168.192.133:8081/nexus/content/repositories/cdel-releases/', //maven仓库地址
            "snapShotUrl": 'http://192.168.192.133:8081/nexus/content/repositories/cdel-snapshots/',
            "groupId"    : 'com.cdel',// 对应maven的GroupId的值
            "userName"   : 'zhanggongzheng',// 登录nexus ossde的用户名
            "passWord"   : '123456',// 登录nexus oss的密码
            "type"       : 'aar',// type
    ]

//    /**
//     * com.cdel:dlconfig:1.0.0           意思为release版本为1.0.0
//     * com.cdel:dlconfig:1.0.0-SNAPSHOT  意思为snapshot版本为1.0.0
//     *
//     * snapshot 此目录下可以为1.0.0-SNAPSHOT或1.0.0+
//     * 1.0.2-SNAPSHOT@aar 关闭依赖关联 @arr和transitive = false作用一样
//     * 1.0.2-SNAPSHOT 打开依赖关联（会同步多个版本的aar，本地缓存导致）
//     *
//     * transitive = true //依赖传递中强制刷新远程库，避免远程库更新后本地未刷新
//     * compile 'common:libsecond:1.0.2-SNAPSHOT'{*     transitive = true //依赖传递中强制刷新远程库，避免远程库更新后本地未刷新
//     *}*/
//    dependencies_version = [
//            "versionDlNet": 'com.cdel:dlnet:1.2.1-SNAPSHOT',
//
//    ]

    // 发布aar相关配置
    maven_lib_dlweex = [
            "artifactId" : 'dlweex',// 对应maven的artifactId的值
            "version"    : '1.0.0.0-SNAPSHOT',// 对应maven的VERSION的值 snapshot应为1.0.0-SNAPSHOT
            "description": 'This is cdel weex lib'// description
    ]

    //关联相关
    dependencies = [
            "support-v4"         : "com.android.support:support-v4:${android.supportSdkVersion}",
            "appcompat-v7"       : "com.android.support:appcompat-v7:${android.supportSdkVersion}",
            "design"             : "com.android.support:design:${android.supportSdkVersion}",
            "support-annotations": "com.android.support:support-annotations:${android.supportSdkVersion}",
            "constraint-layout"  : "com.android.support.constraint:constraint-layout:1.1.2",
            "multidex"           : "com.android.support:multidex:1.0.3",
            "fastjson"           : "com.alibaba:fastjson:1.1.46.android"
    ]
}
